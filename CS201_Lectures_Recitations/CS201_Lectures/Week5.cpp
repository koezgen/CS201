/*
#include <iostream>
#include <string>
using namespace std;
*/

// this is a lecture about classes and constructors.

// there is a method called .length() that returns the number of ASCII characters
// inside a string literal.

/*
int main() 
{
	string string;
	cin >> string;

	cout << string.length() << endl;
	return 0;

}
*/

// header files are glorified dependency listings and function prototypes.

/*
* look for the test.h file in Header Files
*/

// .find() finds the index of a substring. It finds the index of the first char
// of the substring.

// .rfind() does the same thing but in reverse.

// .substr() gets indexes as input and returns a substring. The opposite of .find()

// .at() = "foo" changes the parameter index.

// foo[bar] = "foo" does the same job as .at() member function.

// 
