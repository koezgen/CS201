/*
#include <iostream>
using namespace std;

int main()
{
    //define the type of the variable.
    int numdays;
    float ifahr;

    cin >> numdays;
    cin >> ifahr;
    double dfahr;
    dfahr = (ifahr * 0.9) + 32;
    cout << "There are " << numdays * 3600 * 24 << " Seconds in " << numdays << " days." << endl;
    cout << "F = " << ifahr << endl << "F is " << dfahr << " ." << endl;
    //cout << "Hello, what is your name?" << endl;
    //cin >> name;
    //cout << "Hello " << name << endl;



    int num1, num2;
    cin >> num1 >> num2;

    int numMax = num1;
    // assign flag variables for shorter and efficient codes
    if (num1 < num2) {
        numMax = num2;
    }
    cout << numMax << endl;

    return 0;
}
*/
